# Duck Desktop 使用文档

> 🦆 Hermes Agent 桌面客户端 — 设备监控、多模型切换、Token 追踪、一键更新

---

## 一、快速开始

### 1. 启动应用

双击 `release\Duck Desktop-win32-x64\Duck Desktop.exe` 即可。

或双击项目根目录的 `启动DuckDesktop.bat` 一键启动。

### 2. 全自动启动流程

启动后，应用会**自动完成所有初始化**，无需手动操作：

```
┌─────────────────────────────────────────────┐
│                                             │
│           🦆 小黄鸭 Logo                    │
│                                             │
│           Duck Desktop                      │
│         Hermes Agent 智能助手                │
│                                             │
│           ● ● ● (弹跳动画)                  │
│                                             │
│         正在检测 Hermes 环境...              │
│         Hermes 已安装 ✓                     │
│         正在检查 Hermes Web 服务...          │
│         正在启动 hermes web...               │
│         Hermes 已就绪，正在加载控制面板...    │
│                                             │
└─────────────────────────────────────────────┘
        ↓ 自动进入
┌─────────────────────────────────────────────┐
│       控制面板（全部功能就绪）               │
└─────────────────────────────────────────────┘
```

**全自动流程说明：**

| 步骤 | 自动操作 | 耗时 |
|------|----------|------|
| 1 | 显示启动画面（小黄鸭 Logo + 加载动画） | 即时 |
| 2 | 检测 `hermes` 命令是否可用 | ~1 秒 |
| 3 | 检查 `hermes web` 是否已在运行 | ~1 秒 |
| 4 | 如果未运行，自动启动 `hermes web --port 9119` | ~5-10 秒 |
| 5 | 等待 Uvicorn 就绪后自动进入控制面板 | ~1 秒 |

**首次运行**如果未安装 Hermes：
- 自动检测到并跳转到**环境设置页面**
- 点击「一键安装 Hermes」即可完成安装
- 安装后应用会自动启动 Hermes 服务

---

## 二、功能说明

### 2.1 设备性能监控

实时显示本机硬件状态，每 2 秒刷新：

| 监控项 | 数据来源 | 说明 |
|--------|----------|------|
| **处理器** | WMIC | CPU 使用率百分比 |
| **内存** | WMIC | 已用 / 总内存（MB）及百分比 |
| **磁盘** | WMIC | 磁盘总容量（GB） |
| **网络** | PowerShell | 上传/下载速率（KB/s） |

每个卡片都有**实时折线图**，展示最近 60 个数据点的趋势。

### 2.2 多模型切换

- **当前模型**：显示正在使用的 AI 模型及其能力标签（工具、视觉、推理、上下文窗口等）
- **切换模型**：下拉选择器，支持 18+ 模型提供商，切换后立即生效
- **辅助任务模型**：可为图像识别、网页摘要、上下文压缩等辅助任务单独指定模型

### 2.3 Token 消耗状态

- **总览卡片**：总 Token、输入 Token、输出 Token、预估费用
- **每日柱状图**：按天展示输入/输出 Token 用量
- **热门模型排行**：按 Token 消耗量排序的模型使用统计

### 2.4 版本更新

- **自动检测**：每小时自动检查 Hermes 最新版本（从 hermesagent.org.cn + GitHub 双源）
- **一键更新**：发现新版本后，点击「一键更新」自动下载安装
- **更新日志**：可展开查看新版本的更新内容
- **自动重启**：更新完成后自动重启网关

### 2.5 最近会话

展示最近的 6 个会话，包含：会话标题、模型、来源、消息数、工具调用、Token 用量、最后活跃时间、活跃状态指示灯。

---

## 三、窗口操作

| 操作 | 方式 |
|------|------|
| 拖动窗口 | 按住标题栏区域拖动 |
| 最小化 | 点击标题栏 ─ 按钮 |
| 最大化/还原 | 点击标题栏 □ 按钮 |
| 关闭 | 点击标题栏 ✕ 按钮 |
| 系统托盘 | 关闭后最小化到托盘，右键可显示/退出 |

---

## 四、配置要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 (x64) |
| 内存 | 4 GB RAM |
| 磁盘 | 500 MB 可用空间 |
| 网络 | 首次安装需要网络 |
| Python | 3.11+（Hermes 运行需要） |

---

## 五、常见问题

### Q: 启动后长时间黑屏？

A: Duck Desktop 启动时会显示小黄鸭加载动画。如果长时间黑屏：
1. 确认 `Duck Desktop.exe` 未被杀毒软件拦截
2. 以管理员身份运行试试
3. 应用有 8 秒超时保护，超时后会自动进入控制面板

### Q: 模型切换和 Token 统计显示"暂无数据"？

A: 应用启动时会自动激活 Hermes Web 服务。如果显示无数据：
1. 等待启动画面完成（通常 5-10 秒）
2. 检查 Hermes 是否已安装：终端运行 `hermes --version`
3. 如未安装，首次运行会自动引导安装

### Q: 如何手动启动 Hermes？

A: 在终端运行：
```bash
hermes web --port 9119
```
然后重新打开应用，会自动检测到已运行的服务。

### Q: 设备监控数据全是 0？

A: 设备监控需要 Electron 环境：
- 正式版（exe）：自动通过系统命令采集数据
- 开发模式（浏览器）：部分数据不可用

### Q: 如何重置首次运行设置？

A: 在浏览器控制台执行：
```javascript
localStorage.removeItem("duck-setup-done");
location.reload();
```

---

## 六、技术架构

```
Duck Desktop (Electron)
├── 主进程 (electron/main.cjs)
│   ├── 系统监控 (WMIC / PowerShell / os 模块)
│   ├── 环境安装引导 (electron/setup.cjs)
│   ├── Hermes Web 自动启动 (启动时自动激活)
│   ├── IPC 桥接 (electron/preload.cjs)
│   └── 系统托盘 + 窗口管理
│
├── 渲染进程 (React 19 + TypeScript)
│   ├── Splash 启动画面（显示启动进度）
│   ├── SetupPage 环境设置（首次运行引导）
│   └── Dashboard 控制面板
│       ├── SystemMonitor 设备监控卡片
│       ├── TokenStats Token 消耗统计
│       ├── ModelSwitcher 多模型切换
│       ├── SessionOverview 会话概览
│       └── UpdatePanel 自动更新
│
└── 构建 (Vite 6 + Tailwind CSS 3)
```

---

## 七、开发指南

### 开发模式

```bash
# 安装依赖
npm install

# 启动开发服务器（浏览器预览）
npm run dev:web

# 启动 Electron 开发模式
npm run dev
```

### 打包

```bash
# 构建前端 + 打包 Windows 客户端
npm run build:package
```

产物在 `release/Duck Desktop-win32-x64/` 目录。

---

*Duck Desktop v1.0.0 — powered by Hermes Agent*
